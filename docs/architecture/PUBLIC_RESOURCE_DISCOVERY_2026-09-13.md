# Public resource discovery — September 13, 2026

The owner authorized the existing locality resources to appear in ordinary Resources discovery, using the current marker family, zoom clustering, record details and search, without a regional badge or special heading. Coordination with concurrent mobile visual work is recorded in GitHub issue #288.

The original import only prepared provider identity/promotion candidates. The live discovery service requires published Official Provider profiles, so none of those source records could enter that result set. This change adds an explicit public service-information read model alongside the existing provider projection. A public listing is a separately keyed resource record with no Organization account association. Display does not establish a claim, create a member, grant Official Provider status, infer current availability, publish a participant-authored Resource or enable private requests. Existing provider publication and command authorization remain unchanged.

The generated catalog contains only allowlisted names, service descriptions, websites, aliases, service-area labels and accepted public-office locations from the existing source dataset. It contains 32 listings, of which 22 have accepted Census coordinates. Ten records remain searchable and accessible by website without guessed map positions. Unresolved address and identity review notes stay out of the participant projection. Regenerate with `node scripts/generate-public-resource-catalog.mjs`; this performs no network calls or database writes.

The existing server participant/geography boundary gates discovery. The catalog scope identifies the source operating market; public offices in surrounding localities do not grant participation in those localities. Search covers names, aliases, services and published addresses. Availability is unknown, so an explicit Available/Limited filter does not present these listings as maintaining that state.

Cards, direct resource URLs and marker selection share a record identity. The map adapter clusters the authorized points for Resources at broader zoom levels and leaves the selected marker distinct. It retains every original card and spatial disposition; clustering is presentation only. At close zoom, individual points use the existing marker treatment. Resources uses the concurrent mobile layout's <=1024px width or <=520px height compact breakpoint.

Validation: six new discovery tests cover source-coordinate fidelity, scope/availability denial, server/client search agreement, five-locale card/marker identity, non-authorizing actions, clustering and missing-location behavior. Existing eighteen Resources contract tests pass. TypeScript and focused lint pass. Candidate and merged-main CI remain required before release.

Integration includes visual PR #290 at `8476e0418f0741195e426156af07bca08a1f35aa`, preserving its commit history. Two older source assertions are updated to require the newly authorized tablet/phone-landscape query structure and blue marker accents. Resources uses the same compact layout hook and adaptive map padding; its tablet sheet is explicitly enabled so hiding the legacy panel never hides all results. Review corrections also include public listings in desktop empty-state/count logic, preserve explicit home-locality scope for Opportunities, use white text for solid blue acquisition actions, and leave usable results space in short-landscape sheets. All architecture validators pass on this combined source. No fixture routes or test records are included.

Release: Standard additive discovery/presentation change. Release the final merged main containing the resource and visual candidates with `RFXCHANGE_BUILD_SHA` bound to the same commit and automatic rollouts paused. Retain the previous successful App Hosting build for rollback. There is no data migration to reverse. No independent-assurance label is claimed. At integration time the Cloud Browser connection became unavailable and Firebase CLI had no authorized accounts; this record does not claim a production rollout. GitHub issue #288 and PR #289 carry subsequent CI, merge and release observations.
