# RFxchange modernization delivery

This record distinguishes implementation, release and remaining work for the authorized in-place modernization. The governing target is the SAD v1.1 and Design System v2.0. Firebase project `rfxchange`, existing identities, data and domain authorization remain in place.

## Production hosting — September 11, 2026

The Firebase Console accepted an exact-commit manual rollout of merged main `731521d98587860ca2a6ae7c4fb7e912fb798e0e`. The corresponding successful main CI run is [34637835416](https://github.com/AccelAnalysis/RFxchange/actions/runs/34637835416).

App Hosting now marks `build-2026-09-11-000` as **Current**, with that source commit. The preceding `build-2026-09-05-002` remains the known-good rollback candidate. The public origin loads and `/admin` redirects an unauthenticated visitor to sign-in. Automatic rollouts remain disabled; the backend SHA override was set to the exact source before the rollout.

**2C is not yet complete.** The successful console release does not substitute for the resolved build/rollout API evidence required by `POST_WAVE_3_STABILIZATION_2C_SAME_SHA.md`. Google sign-in and consent succeeded, but the cloud browser blocked the CLI localhost callback. No credential was copied into chat or source. A scoped console read confirms the completed build’s resolved `RFXCHANGE_BUILD_SHA` equals `731521d98587860ca2a6ae7c4fb7e912fb798e0e`, with source Firebase console. Formal source/availability/rollout/READY rollback evidence still needs authenticated API capture and the existing verifier.

## Admin application

`apps/admin` contains the independent build, protected pages, server handlers, dedicated sign-in, bright navigation, sign-out and configured Exchange return link. Shared authorization and audit services are retained. An existing case-transition handler now authenticates before looking up a case, while preserving its subsequent case-specific write checks.

PR #270 is merged as `953abdbbe5731f162800f5b96fefb8ff1e39ce0d`; its main CI run [34643511269](https://github.com/AccelAnalysis/RFxchange/actions/runs/34643511269) passed. Local validation includes the full repository gate and the independent application's production HTTP checks. The `rfxchange-admin` backend is created in `us-east4`, linked to the existing Admin Web App, with root `apps/admin`, branch `main`, automatic rollouts disabled and the exact source SHA override set. The first Admin cloud build (`build-2026-09-11-000`, source `953abdb`) failed with **Missing dependency lock file** before serving traffic. App Hosting treated the npm-workspace directory as a standalone app. The candidate corrects deployment configuration to App Hosting’s supported Nx monorepo format, retaining the repository-root lockfile and shared sources. The actual App Hosting Next.js adapter (14.0.21) successfully built Exchange, Admin and Marketing in an isolated local source copy. All three Next configurations import the shared build-identity module, avoiding an adapter rewrite of one application’s configuration affecting another. Hosted authenticated operation remains pending. Existing root Admin routes stay available until cutover checks pass.

## Marketing application

`apps/marketing` owns the public product/audience pages, membership information, Founding campaign and policy destinations. It has an independent App Hosting configuration bound to the existing Marketing Web App and a bright Design System v2 presentation. Historical price illustrations are omitted; no checkout or commercial entitlement is enabled.

Registration/sign-in handoffs point to the configured Exchange origin and preserve only bounded campaign, supported locale and safe participant return context. Marketing never copies a session cookie. The new Exchange receiver establishes host-only context before registration; authenticated activation binds reported campaign attribution as direct entry, preserving existing referral/invitation/opportunity context and authorization.

The full local gate passed with 954 architecture tests, Functions tests, all three production builds and Admin/Marketing HTTP smoke checks. Marketing smoke covers five locales, public routes, first-touch campaign behavior, credential-free redirects, the Exchange receiver and absence of Admin routes. PR #271 candidate CI [34647266191](https://github.com/AccelAnalysis/RFxchange/actions/runs/34647266191) passed and it merged as `43b48439e16a15f8c4c9891c5acc519599f160b3`. Main CI [34647931161](https://github.com/AccelAnalysis/RFxchange/actions/runs/34647931161) passed. Marketing is now live as `build-2026-09-11-000`, source `ba02ee017f5e371013de1826081685c7b2b293e1`, after the Exchange receiver repair. The backend uses `us-east4`, the existing Marketing Web App, root `apps/marketing` and manual rollouts. Live checks passed for 11 public routes, rejection of `/admin`, registration/sign-in handoff, bounded campaign and locale cookies, and translated French navigation. The hosted desktop presentation was visually inspected. The user confirmed all three live origins are reachable on September 12.

## Remaining delivery

- Capture and verify the formal 2C release evidence.
- Deploy the authenticated Admin entry repair and resolve the signed-in account's administrator-access condition without granting implicit authority.
- Complete the authorized Admin operation check, then release the reconciled application-cutover candidate and inspect its live redirects.

No Feature-ID completion or independent-assurance label is changed by this record.

## Exchange convergence

The shared runtime now applies Design System v2 colors, bright glass, type fallback, readable lens labels and focus treatment. Cartography keeps authoritative geometry, object semantics and camera behavior while replacing ivory/black presentation. The persistent map and sliding sheet composition and RFx/Resources/Intelligence/Capabilities/Menu navigation remain unchanged. Admin and Marketing consume the same shared palette instead of duplicating root overrides. Unused conceptual NetworkField/JourneyRail components and their obsolete global marketing layout are removed; connection semantics remain governed by the shared object tokens. Historical palette checks are reconciled to the approved v2 amendment. The full local gate passed: 954 architecture tests, Functions tests, types, lint, three production builds and Admin/Marketing HTTP smoke checks. PR #272 candidate CI [34648360646](https://github.com/AccelAnalysis/RFxchange/actions/runs/34648360646) and merged-main CI [34648988639](https://github.com/AccelAnalysis/RFxchange/actions/runs/34648988639) passed. It merged as `fcccba16bb55301e3809241aa6e5577c3cb6222f`. Exchange and Admin `build-2026-09-11-001` releases are live on that exact commit, with matching backend SHA overrides. Live Admin sign-in returns 200; `/admin` redirects an unauthenticated visitor to sign-in, and its provider-applications API returns 401. Secure browser sign-in submission did not complete; no hosted authorized operation is claimed.

## Application cutover candidate

The final cutover removes the superseded root Admin pages and public Marketing pages/components. The Exchange keeps participant registration and operations. Legacy Admin navigation redirects to the dedicated Admin host without forwarding query strings or cookies; public entry redirects to Marketing with only bounded campaign and supported locale. Public opportunity pages retain a small shared Exchange header/footer. Existing administrative HTTP implementations are consolidated into shared server modules consumed by both protected API entry points, retaining authorization and audit behavior.

Validation: 955 architecture tests, 45 Functions tests, types, lint and all three builds passed. Admin HTTP checks passed. The extended Marketing/Exchange smoke passed after correcting its parsing of a valid relative locale redirect. It checks legacy redirects, no credential/query forwarding, locale restoration and existing acquisition/session boundaries. PR #274's original head `f06f8fb1ee5e5685d57b2423b7dc5ccac1d73aef` passed CI [34654019370](https://github.com/AccelAnalysis/RFxchange/actions/runs/34654019370). The candidate now incorporates the Admin recovery repair from merged main; fresh exact-head CI and hosted cutover checks are required. The dedicated Admin entry retains the recovery page when the superseded root entry is removed. This candidate must not be released until the independent destinations' applicable checks pass.

## Public-origin receiver repair

Live inspection found App Hosting exposing its internal listener through `request.url`, producing an invalid external redirect from `/acquisition/entry`. PR #273 changes the receiver and historical Founding entry to fixed relative Location headers while preserving context cookies and validation. It merged as `ba02ee017f5e371013de1826081685c7b2b293e1` after candidate CI [34650194204](https://github.com/AccelAnalysis/RFxchange/actions/runs/34650194204) passed. Main CI [34653335472](https://github.com/AccelAnalysis/RFxchange/actions/runs/34653335472) passed. Exchange `build-2026-09-11-002` is live on that commit. Live registration/sign-in redirects, safe participant return paths, rejection of external return paths, and the historical Founding entry passed. Marketing locale restoration uses its fixed public origin because Next.js proxy responses require absolute redirect URLs.

## Authenticated Admin entry repair — September 12

The user completed sign-in and reached a 404 at `/admin`. The route existed: its entry handler deliberately called `notFound()` for a signed-in account lacking a complete administrator profile, active workspace grants, or required privileged-security state. PR #275 replaces that misleading entry error with a bounded recovery page showing the current account and a safe explanation. Authorized destination selection, granular permissions, server checks and audit behavior are unchanged. It adds no role, grant, bypass or account binding.

The full local gate passed and candidate CI [34663531931](https://github.com/AccelAnalysis/RFxchange/actions/runs/34663531931) passed on `3ad058eca0a9a984c6094c81ba7653ac04a0196d`. The repair merged as `de61a257efaabff8d478fae6c89f12f1d0251712`; its main CI and exact-commit deployment are in progress. Live inspection found the existing administrator lifecycle profiles were old configured-acceptance accounts; a test profile must not be rebound to a production user. The signed-in account's condition still needs a live check after the repair deploys. Any new consequential access grant needs an explicit account, permission/scope decision and auditable provisioning; successful sign-in alone is not authorization.
