# RFx Core documentation authority

**Status: canonical planning index after merge; no RFx Core implementation is authorized by this documentation alone.**

This directory contains the converged product, architecture, AMACS, UX, feature-crosswalk and acceptance authority for the future live RFx Core.

The governing decision is:

> One structured RFx transaction is presented through role-appropriate issuer, responder, teammate and later evaluator workspaces. AMACS supplies versioned market-capability, request-family, response-structure, evaluation-method and readiness semantics. The RFxchange supplies organization authority, geography, lifecycle, workflow, permissions, projections and participant experience.

## Read in this order

1. [`RFX_CORE_AMACS_CONVERGENCE.md`](RFX_CORE_AMACS_CONVERGENCE.md) — final convergence decisions and scope boundaries.
2. [`AMACS_INTEGRATION_CONTRACT.md`](AMACS_INTEGRATION_CONTRACT.md) — versioned AMACS ingestion, runtime projection, search and snapshot rules.
3. [`RFX_CORE_FEATURE_CROSSWALK.md`](RFX_CORE_FEATURE_CROSSWALK.md) — all 41 Wave 4 Feature IDs accounted for exactly once.
4. [`RFX_CORE_PRODUCT_WORKSPACES.md`](RFX_CORE_PRODUCT_WORKSPACES.md) — issuer, responder and teammate workspace/interaction authority.
5. [`RFX_CORE_ACCEPTANCE_MATRIX.md`](RFX_CORE_ACCEPTANCE_MATRIX.md) — pre-merge acceptance and anti-regression requirements.
6. [`../slices/WAVE_4_RFX_CORE_ROADMAP.md`](../slices/WAVE_4_RFX_CORE_ROADMAP.md) — adopted planning sequence; implementation remains not started.
7. [`../brand/BRAND_GATE_B6C_RFX_LENS.md`](../brand/BRAND_GATE_B6C_RFX_LENS.md) — real-data RFx spatial expression and map boundaries.
8. [`../reference/prototypes/RFX_VERSION_2_DESIGN_REFERENCE.md`](../reference/prototypes/RFX_VERSION_2_DESIGN_REFERENCE.md) — approved prototype-derived visual decisions and explicit non-production mechanics.

## Authority order

For future RFx implementation:

1. current authorized slice/task;
2. security, privacy, organization authority, geography, lifecycle, tracker and dependency authority;
3. the applicable Wave 4 slice brief;
4. this RFx Core documentation package;
5. `docs/brand/` target experience;
6. `docs/design/` converged implementation baseline;
7. existing production abstractions and tests;
8. prototype references only for the decisions explicitly adopted by their companion notes.

## Explicit boundaries

This package:

- changes no Feature-ID status or tracker total;
- creates no live RFx, opportunity, pursuit, response, team, submission, evaluation, award or outcome record;
- adds no fake map, organization, opportunity, statistics or market activity;
- does not import prototype HTML/CSS/JavaScript into production;
- does not expose AMACS implementation identifiers as ordinary participant language;
- does not authorize Wave 4 before the current Wave 3 sequence and explicit implementation authorization are complete;
- does not move Wave 5 evaluator/award capabilities into Wave 4.
