# Verification record

## Reproducible focused gate

```bash
node scripts/mobile-exchange-stage4-intelligence-acceptance.mjs
```

The focused gate runs the two Stage 4 Intelligence suites plus the merged Stage 3 shared-contract and map-projection suites. Repository typecheck and lint are run separately under the pinned Node 24.18.x toolchain.

## Acceptance mapping

| Acceptance | Objective evidence |
| --- | --- |
| Functional | Ready, empty, restricted and focused-result projections; stable card/map/detail identity; canonical return target; exactly four actions. |
| Domain security | Exact geography binding; stale selected identity denial; independently authorized focused selection; fail-closed non-ready projections; four-scope allow/deny matrix; hidden unsupported mutations. |
| Browser visual | Existing configured Exchange-shell acceptance exercises `/geography/canvas`; this packet adds no new shell or presentation primitive. Exact candidate CI remains the authoritative configured-browser record. |
| Responsive | Existing shared desktop edge sheet/mobile bottom sheet remains unchanged and receives the same validated authoritative discovery. |
| Accessibility | Localized accessible card labels, privacy-safe marker labels, and existing keyboard/list shared contracts are preserved. Stage 3 map tests remain in the focused gate. |
| Copy | Five locale projections cover source, record type, visibility, geography, vintage, quality and coverage language without unsupported market claims. |
| Cross-lens | Stage 3 shared contract/map tests run with the Intelligence suites; no private lens framework or cross-lens state store is introduced. |
| Performance | The bounded 250-candidate stress projection is synchronous and asserted below 500 ms locally; production discovery remains capped and paginated. |
| Five locales | `en-US`, `es`, `fr`, `it`, and `de` produce stable identities with localized projection copy. |
| Exact-head CI | Record the production-CI run for the final PR head in the PR evidence; do not infer it from a prior SHA. |
| Configured browser | The production-CI configured Exchange-shell stage is required on the final PR head. |

## Local evidence before publication

- Stage 4 Intelligence focused suites: 11/11 passed.
- Focused Stage 4 + Stage 3 shared contract/map gate: 20/20 passed.
- Configured Exchange-shell acceptance: passed on the final working tree after exercising the real `/geography/canvas` server boundary; five locales, 390 px mobile with zero overflow, reduced motion, clean console/exceptions, preserved Intelligence context, zero shell remounts, zero root takeovers, median candidate transition 92.3 ms and p90 161.6 ms.
- Canonical `npm run check`: passed; 784/784 repository architecture tests, Functions build/tests, typecheck, lint with zero errors, and production build all passed.
- Typecheck: passed with Node 24.18.x.
- Lint: passed with zero errors; pre-existing repository warnings only.
- Starting-main dependency/post-merge CI: run `32109036958` succeeded on `8f348c8c86a2a8eb1eeb6402a170a9120824d7ae`.

Exact final candidate SHA, draft PR URL, exact-head CI, and any configured-browser artifact are recorded in the draft PR after publication. They are not predeclared here because candidate identity is immutable evidence.

## Negative evidence

The suites prove that this packet does not:

- admit client-derived visibility authority;
- cross private user, team, or organization boundaries;
- retain protected result sets or markers in restricted state;
- admit a stale selected organization or wrong-geography focused result;
- fabricate a missing Intelligence layer, note, comparison, follow handler, market measure, or analytical statistic;
- rename/reorder the current immutable action IDs; or
- substitute PR #220’s legacy Stage 2 contracts for merged Stage 3 contracts.
