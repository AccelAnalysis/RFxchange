# RFx Slice 4.7 builder evidence

This directory records builder/repository evidence for `WP-MOBILE-EXCHANGE-RFX-47-01` and Feature IDs `DSC-010`, `RSP-007`, `RSP-008`, `TEM-001`, `TEM-002`, `TEM-003`, `TEM-004`, and `ACQ-007`.

The packet was activated from Control Room merge `8f348c8c86a2a8eb1eeb6402a170a9120824d7ae` and reconciled to the narrow ownership-amendment merge `4a3d9042425b88170f16cbda6aff61ba875abea1`. The implementation branch is `codex/rfx-slice-4-7`. Its pull request body and exact-head production CI bind the final candidate; the later Control Room closeout must record that immutable candidate, merge, post-merge CI, and only the Feature IDs whose own completion evidence remains satisfied.

## Result

- A current permitted `pursue` relation and current team-coverable missing/unconfirmed capability gap produce one immutable server-derived teammate context. Stale, resolved, deferred, non-coverable, wrong-tenant, withdrawn, expired, or non-pursue state fails closed.
- Teammate results reuse the authorized Network discovery and controlled map projection. Only current privacy-safe organization projections with an exact confirmed structured capability match are candidates; the acting and issuing organizations are excluded. The API re-runs that same server discovery for the chosen organization and never accepts a client-supplied candidate projection.
- Resource handoff carries only bounded opaque `rfxReference`, `rfxGap`, and same-opportunity `returnTo` context into the existing Resources boundary. It grants no provider, request, RFx, or return authority.
- A versioned organization-owned invitation records minimum RFx/gap provenance, one organization or external recipient, proposed capacity, bounded responsibility, command fingerprint, immutable events, and actor evidence. Create and decision transactions revalidate the current opportunity, pursuit, gap, restrictions, membership, and `response.create` permission.
- External creation atomically prepares the existing `team-invitation` acquisition envelope and communication intent. The delivered secret is not authority. The already-bound acquisition continuation returns to the exact review route without attachment or acceptance.
- Review and decision require the exact invited organization or an exact acquisition subject plus matching current email and organization authority. Accept records only RFx-scoped participation after the current five-locale nonbinding boundary is acknowledged; it grants no response, submission, issuer, qualification, legal teaming, or compensation authority.
- An existing-organization inbox and exact review route remain inside the Opportunities/RFx participant shell. Empty, unavailable, stale, dependency, pending, accepted, declined, revoked, and expired states remain truthful.

No response construction, assignment, submission, provider mutation, referral lifecycle, shared participant contract, tracker, governance, deployment, B6c, B6b, appearance, or Stabilization 2C change is included.

## Evidence state

- Focused domain/workspace tests: 13/13 passed.
- Firestore emulator: atomic internal/external creation, exact replay, acquisition binding, localized acceptance evidence, direct-client denial, and cleanup passed.
- Canonical local gate: all validators, Functions build/tests, 786 architecture tests, typecheck, and lint passed. The final local Turbopack invocation is not evidence because the isolated worktree intentionally shares `node_modules` by an out-of-root symlink; a prior standalone clean-source production build passed. Exact-head CI must supply the authoritative clean-checkout production build.
- Configured-browser evidence in canonical CI remains a shell/accessibility/transition regression. It does not by itself claim a live external email delivery or production-network invitation journey; provider delivery and production release remain separately controlled.
- Assurance: builder/repository evidence only. No optional independent assurance event has occurred, so no `Verified` claim is made.
