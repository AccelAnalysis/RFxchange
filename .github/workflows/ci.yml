name: production-ci

on:
  pull_request:

permissions:
  contents: write

jobs:
  sync-pr249:
    if: github.head_ref == 'control/geography-fabric-resolver-enrichment'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Synchronize stacked resolver packet
        run: |
          git config user.name "RFxchange implementation"
          git config user.email "actions@users.noreply.github.com"
          python .github/scripts/sync-pr249.py
