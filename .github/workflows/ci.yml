name: provider-promotion-diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Capture isolated test output
        shell: bash
        run: |
          mkdir -p artifacts
          set +e
          node --test test/provider-seeding-firebase-promotion.test.mjs > artifacts/provider-promotion-test.log 2>&1
          status=$?
          set -e
          printf '%s\n' "$status" > artifacts/provider-promotion-test.status
          cat artifacts/provider-promotion-test.log
      - name: Upload isolated test diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: provider-promotion-test-diagnostics-${{ github.event.pull_request.head.sha }}
          path: artifacts/provider-promotion-test.*
          if-no-files-found: error
          retention-days: 3
